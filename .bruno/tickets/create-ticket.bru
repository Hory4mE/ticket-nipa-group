meta {
  name: create-ticket
  type: http
  seq: 2
}

post {
  url: {{HOST}}:{{PORT}}/v1/tickets
  body: json
  auth: none
}

headers {
  token: {{token}}
}

body:json {
  {
    "title": "just created",
    "description": "test"
  }
}
